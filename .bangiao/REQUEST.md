# Request

Implement the complete VIDORA_MANUAL_MODE_CODEX_TASK_AND_SHIP_PROMPT.md on this repository, in WP0–WP9 order, meeting every Definition of Done and test acceptance criterion. The source task is user-provided and must be preserved.

Branch: fix/manual-mode-primary-workflow. Commit in logical groups, push and open a PR; do not merge.

Baseline HEAD: a82fcc082bb3268fef11be83fca0c9d3428197d8 (matches audit baseline). Initial worktree: only the user-provided task file is untracked; no pre-existing tracked modifications. No root AGENTS.md file exists; follow the AGENTS instructions supplied in the conversation: use Semble before code exploration (CLI available).

User explicitly authorized continuing with the runtime's current fixed role/model configuration, overriding the task's model restriction. Run the ship Planner → Coder → Tester → Reviewer workflow with independent validation and review.

Manual workflow is the production default; users alone send/attach on ChatGPT. Main-process guard, durable attempts, proven ownership, canonical bundles, disk audit, restart/resume and strict N/N VeoUp gating are mandatory. Preserve legacy project assets, consolidate duplicate engines, add behavioral characterization and integration tests, update documentation, and report tests honestly.
